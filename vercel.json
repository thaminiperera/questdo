{
  "buildCommand": "cd frontend && yarn install && npm run build",
  "outputDirectory": "frontend/build",
  "frameworks": "other",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/index.js"
    }
  ]
}
